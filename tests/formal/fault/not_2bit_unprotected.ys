# Performs fault injection for the not_2bit circuit

# Original circuit
[gold]
read_verilog -sv ../tests/verilog/not_2bit.sv
prep -top inverter
rename -top design
splitcells
splitnets

# With injected faults
[gate]
read_verilog -sv ../tests/verilog/not_2bit.sv
prep -top inverter
rename -top design
splitcells
splitnets

# select only input signals
select % a:tamara_voter %d
# apply a random mutation (fault injection)
mutate -list 1 -o /tmp/tamara_fault_injection
# deselect, go back to top module
select -clear
# execute the fault injection command
script /tmp/tamara_fault_injection

[strategy sby]
use sby
depth 2
engine smtbmc yices
